# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`主版本.次版本.修订号`。

---

## [1.4.0] — 2025

**主题：** 把"音乐播放器"真正长进莱茵生命终端里 —— 右侧那一栏不再是档案术语，
底部的播放条也不再是另一套坐标系与另一套配色。

### 修复

**★ 详情区把歌曲硬塞进档案字段（用户："不要生搬硬套原有UI"）**

用户给的是一张 793×744 的截图，而本会话读不了图。于是先量出"它究竟是什么"：
截图里的暗色块均色 `#252820`、高 46px、左边距 1154px、顶边 289px —— 与源码里
`.detail-content { left:1154px; top:289px; width:636px }` 和 `.solid-button`
（`background:#252820; height:46px`）逐项吻合，即**详情区最下面那一行动作按钮**。
据此可以确定问题出在右侧详情栏的内容映射上：

```
之前的映射（把歌曲当档案显示）：
FILE X-001 / AUTHORIZED            ← 档案编号 / 权限
<h2>艺术家名（大写英文）</h2>        ← 原本放档案英文名
DEPARTMENT / 科室      → 艺术家      ← 字段名与内容完全不对位
COLLECTION / 编目范围  → AUDIO
RELATED / 相关人物     → 专辑
STATUS / 状态          → 已归档 · 可读取
页签 01 概述 / 02 研究记录 / 03 访问日志
```

现在这一栏按播放器该有的样子重做，同时保留终端自己的网格与字号
（`.detail-content` 依旧 1154 / 289 / 636）：

| 区域 | 现在显示 |
| --- | --- |
| 顶部编号行 | `TRACK 003` ＋ `PLAYING · LOOP`（播放状态与播放模式） |
| 头部 | 封面 130×130（ID3 / 内嵌图，缺失时用程序生成的默认封面）＋ 曲名（`h2`）＋ 艺术家 · 专辑 |
| 元数据四格 | 时长 / 播放次数 / 来源（`FLAC`、`NCM → MP3`、`B 站缓存`）/ 上次进度 |
| 实时频谱 | `<canvas>` 636×72（2 倍超采样）：127 根对数分频柱 ＋ 静默基线 ＋ 暖色峰值保持线 |
| 页签 | `01 曲目` / `02 歌词` / `03 播放记录` |
| 歌词页签 | LRC 逐行带时间戳，当前行高亮并自动滚动，**点任意一行跳转到该句** |
| 动作行 | `＋ SAVE TRACK ／ 收藏曲目`（沿用终端 `.solid-button` 的实心深色与回弹动画）＋ `PLAY ▶ ／ PAUSE ■` |

**★ 底部播放条与界面割裂（用户："界面下方的播放栏风格与UI风格十分割裂"）**

根因有两条，都能量出来：

1. **坐标系不同**：终端整套界面挂在 `#stage`（1920×1080，按
   `scale(min(w/1920, h/1080))` 整体缩放）里，而播放条挂在 `document.body` 上 ——
   窗口一缩放，界面在缩放、播放条不缩放。Electron 默认窗口 1380×920，
   缩放系数 0.719：界面 71.9%，播放条 100%。
2. **配色不同**：终端是纸面 `#e8e5e1` ＋ 细线 `#c1b8a7` ＋ 实心 `#252820 / #f0eee5`
   ＋ 暖色强调 `#9b7247` / 橄榄 `#565c46`；播放条用的却是一张参考图里的黄铜 `#C5A974`。

现在播放条也挂进 `#stage`，并对齐终端自己的网格
（`left:59px; bottom:124px; width:880px; height:104px`，与右侧 `.column-navigation`
共用同一条底线），配色改回终端自己的设计语言：

- 深色实心播放键 = `.solid-button` 的 `#252820 / #f0eee5`；其余按钮 = `#f5f2e9` ＋ 1px `#c1b8a7`，
  悬停转橄榄 `#565c46`
- 全部直角、9px 全大写字距标签、MiSans、10px 等宽数字时间，与终端一致
- 播放条右侧一条 **52 格实时电平刻度**（2px 宽，与 `.file-ticks` 是同一种刻度语言）
- 昼 / 夜 `◐` 的夜态换成终端自己的深色家族（`#252820` / `#30362a` / `#4b4a3b` / `#f0eee5`），
  不再是参考图那套偏蓝的青石板

### 新增

- **第二条可视化**：播放条电平刻度 ＋ 详情区频谱，由同一个 `AnalyserNode` 驱动。
  取不到运行中的 `AudioContext` 时**不接管音频元素** —— 宁可没有频谱，也不能没有声音。
- **阵列与详情跟随播放**：`playAt()` 派发 `rhine-track`，三维档案阵列的选中态与右侧详情
  会一起移到正在播放的那一首（播放列表与档案阵列本来就是同一份数据）。
- **歌词异步补全**：导入后由主进程匹配到 LRC 时派发 `rhine-lyrics`，只重绘歌词页签。

### 验证

量测方式：无头 Edge 渲染 + 页面把 `getBoundingClientRect` 主动 POST 回验证服务
（受限环境里拿不到 `--dump-dom` 的 stdout）。

```
detail    1139,242 636x721  end 963      ← 详情区底边在 stage y=1010（1080 内，余 70）
songhead  1139,281 636x130  (cover 130x130)
metadata  1139,463 636x106  ← 两列
songviz   1139,593 636x116  (canvas 636x72)
tabs      1139,709 636x35
tabpanel  1139,744 636x122
actions   1139,885 636x46
deck        44,805 880x104  ← .p-top/.p-mid/.p-bot = 38/22/20
objcap      45,596 158x202  end 798     ← 与播放条顶边 805 相距 7px，不重叠
footer      44,984 1802x14
playlist    44,463 880x332
```

ASCII 渲染确认：封面、`.detail-rule` 细线、两列元数据、频谱柱、深色播放键 ＋ 浅色三角、
52 格电平刻度、进度条都按设计出现。`tsc && vite build`：62 modules。

### 说明

- 播放列表仍是覆盖式浮层（`left:59px; bottom:238px; width:880px`）。在详情视图里它会盖住
  左下的 `.object-caption`；关掉即恢复。彻底避免需要重排终端原有版式，暂不做。
- 终端自身的片头背景音乐与播放器音轨互不影响（两套音频对象）。若希望"开始播放音乐时
  自动静音终端背景音乐"，可以再单独加一条联动。

---

## [1.3.1] — 2025

**主题：** 修掉"开了封面 3D 但什么都看不到"的真 bug，并给项目装上**能自己看界面**的验证工具。

### 修复

**★ 封面 3D 的绘图缓冲被设成 1×1，导致一帧都画不出来**

用户反馈"我并没有看到 3D 效果"。这一次没有靠猜 —— 我搭了一个离线桩环境把
`app/index.html` 在无头浏览器里渲染出来，dump DOM 后读到了确凿证据：

```
<canvas id="cover3d" width="1" height="1" class="on">     ← 绘图缓冲只有 1×1 像素
<canvas id="viz"      width="600" height="312">           ← 对比：可视化画布正常
```

根因是 `c3dEnable()` 里的**调用顺序**：

```
c3dResize();                     ← 此刻 #cover3d 还是 display:none
cv.classList.add('on');          ← 之后才显示
```

`display:none` 时 `clientWidth`/`getBoundingClientRect()` 都是 **0**，
而原代码写的是 `cv.clientWidth || 1`，于是取到 1 →
`setSize(1,1)`、`camera.aspect = 0/1 = 0` → 渲染到 1×1 缓冲，屏幕上什么都不会出现。
只有手动缩放窗口（触发 resize 监听）才会突然冒出来。

修复三步：

1. **先显示、后测量** —— 把 `cv.classList.add('on')` 提到 `c3dResize()` 之前
2. `c3dResize()` 改用 `getBoundingClientRect()`，量到 0 时**用父容器兜底**
3. 仍为 0 就返回 false，调用方**下一帧重试**（最多两次）

验证（同一桩环境、同样参数，只看画布属性）：
```
修复前: width="1"   height="1"
修复后: width="410" height="301"
```
并用 ASCII 渲染确认封面区从"均匀暗块"变成了有明暗层次的三维物体。

### 新增

**项目自己"看界面"的能力（`scripts\渲染快照.ps1` + `scripts\截图转文本.js`）**

本项目的 UI 验证一直只能靠人看；而沙箱里既起不了带界面的 Electron，
也没有可用的视觉模型（`claude` CLI 未登录、`opencode` 起不来、`modlens` 未安装）。
于是把三步串成一条命令：

```
1. 起本地 HTTP 静态服务承载构建产物（file:// 下 module script 与绝对路径都会失败）
2. 无头 Edge 渲染并截图
3. 把 PNG 解码成 ASCII 灰度图打印出来 —— 读不了图片的会话也能实际看到布局
```

第 3 步是纯 Node 实现的 PNG 解码（`zlib` + 逐行反滤波，无第三方依赖），
支持 8 位 RGB/RGBA/灰度/调色板、非隔行。

这套工具在这次排查里起到了决定性作用 —— 上面那条 `width="1"` 的证据就是它拿到的。

### 说明

`scripts\渲染快照.ps1` 里记了五个真踩过的坑，都写在代码注释里：
`Start-Process -ArgumentList` 不会自动给含空格的路径加引号（本仓库路径含空格）、
Chrome 在本机无法使用其数据目录而 Edge 正常、浏览器写 stderr 的告警会被 `EAP=Stop` 当致命错误、
`--screenshot=相对路径` 按浏览器自己的 CWD 解析、以及浏览器进程返回时截图尚未落盘（必须轮询等待）。

---

## [1.3.0] — 2025

**主题：** 把第二个 Blender 场景（拆解件）也用起来 —— 封面 3D 现在可以在
**装好的档案**与**拆开的内部结构**之间切换，并把转速与播放状态联动。

### 新增

**封面 3D「拆解 / 重组」交互**

封面左下角两颗键：「拆解」（把档案沿 Z 轴分层散开，露出内部六个部件）、
「复位」（装回去并把旋转角归零）。

拆解动画**完全照抄 RhineLabUI `model-viewer.ts` 的规范**，不是自行设计：

```
PARTS = [ {id, depth} × 6 ]                        六个部件各带一个 depth
按 userData.assemblyPart 把网格分组                 22 个节点每个都带这个 extras
每帧 group[part].position.z = depth * spread       沿 Z 平移
spread 阻尼跟随目标值，速率 5.5
```

| 部件 | 英文 | depth |
| --- | --- | --- |
| 紧固件 | FASTENERS | 2.75 |
| 透明盖板 | OPTICAL COVER | 1.85 |
| 折射环组 | REFRACTIVE RINGS | 0.75 |
| 光学核心 | OPTICAL CORE | −0.15 |
| 信息基板 | SUBSTRATE | −1.10 |
| 背板与框架 | CARRIER | −2.05 |

- **两个模型共用同一个中心偏移**（都按单体的包围盒居中）：各自居中的话，
  拆解时零件会整体错位 —— 它们出自同一套 Blender 坐标
- 装好时显示单体，`spread > 0.02` 时换成拆解件；两者在 `spread = 0` 时外形一致，
  硬切换看不出来
- **取景只做一次**，按单体包围盒定相机（1.72 倍留白吃掉拆解增量），
  拆解途中不重新取景 —— 否则镜头自己缩放会晕

**转速与播放状态联动**

播放时正常转（0.30 rad/s），暂停时几乎停住（0.05），阻尼过渡避免一按暂停就跳。
这样 3D 与"正在放歌"产生联系，而不是自顾自地转。

**莱茵生命主题下默认开启**

那套主题的封面本来就该是这卷档案。其它主题仍默认关闭；
**用户只要手动关过一次，就永远尊重用户的选择、不再自动打开**。

### 说明：`.blend` 与 `.glb` 的关系

用户要求"把 Blender 文件用于播放器 UI"。需要说清一件事：
**`.blend` 是 Blender 的工程文件，运行中的程序读不了它** —— 没有运行时会去解析 `.blend`。
正确通路是在 Blender 里导出 `glTF 二进制`（`.glb`），程序加载 `.glb`。

压缩包里已经带了导出好的两个 `.glb`，与那两个 `.blend` 场景一一对应
（原项目 `AGENTS.md` 写明是"通过 Blender MCP 重新生成单体与拆解资产，保留
`art/internal_architecture.py` 和两个 `.blend` 源场景"）：

| `.blend` 源 | 导出件 | 用途 |
| --- | --- | --- |
| `rhine-archive.blend` | `archive-cassette.glb` | 单体（默认视图）—— v1.2.0 已用 |
| `archive-assembly.blend` | `archive-assembly.glb` | 拆解件 —— **本版本新用** |

本机没装 Blender，所以无法从 `.blend` 重新导出或减面；直接用现成 `.glb` 是唯一可行做法。

### 变更

- `app\package.json`：版本号 `1.2.0` → **`1.3.0`**（新增交互，走次版本号）
- `app\assets\archive-assembly.glb` 新增（3.2 MB），应用体积 `5.2 MB → 8.4 MB`
- `docs\功能说明.md` §10 重写：`.blend`/`.glb` 关系、拆解规范、部件表、联动与默认策略

---

## [1.2.0] — 2025

**主题：** 把用户提供的 RhineLabUI 压缩包素材真正用起来 —— MiSans 字体（按需命中）、
封面 3D 档案磁带，以及若干排版收尾。

### 新增

**封面 3D 磁带（默认关闭，⚙ 面板可开关，选择会记住）**

在封面区叠一层可旋转的 3D「档案磁带」，模型是 RhineLabUI 的
`archive-cassette.glb`（MIT）。**透明底叠加**：`alpha:true` + `setClearAlpha(0)`，
专辑封面从底下透出来，磁带像浮在封面上。

- **自动取景**：模型尺寸与原点未知，按 `Box3` 包围盒算中心与半径再定相机距离
- **材质配色照抄原项目**：模型无贴图（17 材质 / 约 8.6 万三角形），材质名与
  RhineLabUI `theme-material.ts` 的 `surfaces` 表一一对应（`Frosted_Polymer #626b70`、
  `Optical_Diffuser #192226`、`Amber_Optical_Inlay #c6a36b` …），直接沿用那套配色，
  未知材质按名字兜底
- **省 GPU 的措施**：按需注入脚本（不开启一行都不加载）、上次开启时延迟 1.5 秒再加载、
  限帧 30fps、窗口最小化/隐藏即停（接 `win-state`）、像素比封顶 1.5
- **失败自关**：WebGL 不可用 / 模型读不到 / 脚本加载失败 → 提示一次并自动关闭，封面回 2D

**新增 IPC `read-asset`（只读 + 白名单）**

页面是 `file://` 加载的，而 Chromium 禁止 `file://` 页面对本地文件发 XHR/fetch，
`GLTFLoader.load()` 会撞 CORS。因此由主进程读字节、渲染进程 `loader.parse()`。
该通道**只读且只放行白名单文件名**（不接受任意路径），遵守既有的数据管控结构性保证。

**MiSans 字体（不内置，系统已装则自动生效）**

用 `@font-face` + `local()`：装了就命中 MiSans（RhineLabUI 用的就是它），
没装则整条规则失效、自然回落微软雅黑。

> 为什么不做子集化：子集化 = **修改字体文件**，而 MiSans 不是 MIT（是小米自己的授权），
> 其许可 PDF 本身是子集化的、条款无法提取，无法确认是否允许修改。所以选零体积、
> 零授权风险的 `local()` 方案。想给四套主题都用上，去掉那条选择器里的
> `[data-theme="rhine"]` 即可。

**依赖与资源（新增约 4.1 MB）**

| 文件 | 体积 | 说明 |
| --- | --- | --- |
| `app/vendor/three.min.js` | 593 KB | three r147 —— **最后一个带 UMD 构建的版本** |
| `app/vendor/GLTFLoader.js` | 101 KB | 对应的 classic 版 loader |
| `app/assets/archive-cassette.glb` | 3.4 MB | 磁带模型 |

> 为什么锁 r147：页面跑在 `file://` 下，**ES module 会被 CORS 拦**，
> 所以不能用现代 three.js（ESM-only），必须用 classic script（UMD）。
> r147 是最后一个同时提供 `build/three.min.js` 与 `examples/js/loaders/GLTFLoader.js` 的版本。

### 变更

- `app\package.json`：版本号 `1.1.6` → **`1.2.0`**（新增功能，走次版本号）
- 文档里写死的 IPC 数量同步：`27 个通道 → 28 个`、`preload 16 个方法 → 17 个`
  （`docs\数据管控与合规.md` / `docs\架构说明.md` / `README.md`）
- `docs\构建与安装.md`：磁盘需求里「源码 1 MB」→「应用源码约 5 MB」

### 说明

用户提供的 `RhineLabUI_源码与Blender工程_MIT_2026-09-09.zip`（47MB）是**参考素材**，
`*.zip` 已在 `.gitignore` 里，不会进仓库。本次只取用了：模型的材质配色表、
`archive-cassette.glb`、`src/style.css` 里的排版/交互手法（见 [1.1.6]）。
Blender 工程（`.blend`）**未使用** —— 播放器不需要 3D 创作流程。

---

## [1.1.6] — 2025

**主题：** 把 RhineLabUI 里"与主题无关、但对四套主题都是纯改善"的交互细节搬过来；
并修掉文档检查器的一个误报。

### 新增

从 RhineLabUI 的 `src/style.css` 里抄了三处本项**之前一直缺**的做法（配色全部走主题变量，
所以四套主题都适配）：

| 项目 | 之前 | 现在 |
| --- | --- | --- |
| **细滚动条** | 完全没有样式，Chromium 默认滚动条在深色底上非常突兀（播放列表尤其明显） | `::-webkit-scrollbar` 9px、`background-clip:content-box` 的细滑块，颜色取 `--card-border` / `--muted` |
| **键盘焦点环** | 完全没有 `:focus-visible` 样式 —— 纯键盘操作时根本看不到焦点在哪 | `outline: 2px solid var(--accent); outline-offset: 3px`（大按钮 4px）。原规范用 2px 实线 + 较大外偏移，让焦点框不贴着控件边框，小按钮上特别清楚 |
| **`::selection`** | 用浏览器默认蓝，与主题不搭 | 底色 `--accent`、文字 `--bg` |

莱茵生命主题另有两处收尾：滚动条滑块收直角；浮动小标签（`#hkLabel` / `#dcLabel` /
`#verLabel` / `#listBtnCount`）加 `.06em` 字距 —— 原规范里所有小号标签都带 0.7~1.3px 字距，
但**中文标题不加**（中文加字距会散），只给数字与短标签用。

### 修复

- **文档一致性验证器不遵守 `.gitignore`**：它靠遍历文件系统取受管文件
  （为的是不 spawn git 子进程 —— 受限环境下 `spawnSync git` 会 EPERM），
  但只跳过了 `.git`，于是仓库里放了一个**被忽略**的 47MB 素材压缩包
  （`*.zip` 在 `.gitignore` 里）之后，检查器就要求把那个 zip 写进 README 结构图 ——
  而它本来就不该进仓库。已实现一份够用的 `.gitignore` 匹配
  （目录名 / `*.ext` 通配 / 相对路径 / `!` 反选），并报告忽略条目数。
  现在结果与 `git ls-files` 一致，仍然不需要 git 子进程。

### 变更

- `app\package.json`：版本号 `1.1.5` → **`1.1.6`**

### 说明

`RhineLabUI_源码与Blender工程_MIT_2026-09-09.zip`（47MB，含 `art/*.blend`、`public/assets/*.glb`、
MiSans 字体与源码）是**参考素材**，`*.zip` 已在 `.gitignore` 里，**不会进仓库**；
本项目只借用配色值与设计原则，未搬运其代码与素材。

---

## [1.1.5] — 2025

**主题：** 新增第四套主题「**莱茵生命**」——把
[RhineLabUI](https://github.com/LBEILC/RhineLabUI)（明日方舟 · 莱茵生命，MIT）的
视觉规范合理地落进播放器。

### 新增

**主题「莱茵生命」（`rhine`）**

不是简单地加一组颜色，而是按对方的规范把**材质**也一起搬过来。取材依据是它自己
`DESIGN.md` 的「明暗配色」一节：

> 暗色以 `#11181b` 为背景，`#e0e3dc` 为正文，`#a6b0b1` 为辅助文字，
> `#536166` 为分隔线，`#c5a16b` 为琥珀强调。

落到播放器上的三件事：

| 做法 | 实现 |
| --- | --- |
| 配色 | `applyTheme()` 新增 `rhine` 分支：bg `#11181b` / panel `#202a2f` / card `#2a363b` / card-border `#536166` / text `#e0e3dc` / muted `#a6b0b1` / accent `#c5a16b` / accent2 `#9b7247`；`--hue` 设 38，让 `hsla(var(--hue) 85% 62%)` 那批选中态自然变成暖琥珀 |
| 材质 | 1px **实心**细线代替半透明描边；按钮、pill、列表项、封面、可视化区一律 `border-radius: 0`（原规范的按钮就是 1px 描边 + 无圆角），大面板留 2px |
| 选择语义 | 原规范用「长短竖线刻度」表达选中。列表选中项因此改成**左侧黄铜竖条 + 极淡底**，而不是整块色块填充 |

另外：数字用等宽字形（`tabular-nums`，时间读数不随数字宽度左右抖）；
封面四角加黄铜直角标记（一个伪元素用 8 条 1px 渐变画四个角）。

**刻意保留圆形**的是播放键、收藏键与圆形滑钮 —— 圆形是"可点"的强提示，
而且是早先明确要求过的（"暂停键不要方"）。混搭是有意的，代码注释里写明了。

**实现要点**

- `applyTheme()` 现在会把主题名写入 `document.documentElement.dataset.theme`。
  这是必需的：换色只能改 CSS 变量，而**材质差异必须靠 `:root[data-theme="rhine"]`
  限域规则**才能生效 —— 没有这个钩子就只是"干换颜色"。
- 小窗无需改动：它从 meta 里拿 `c1 = --accent2` / `c2 = --accent`，
  黄铜色会自动同步过去。

### 修复

- **`::after` 的 stacking 陷阱**：封面四角标记原本写成 `z-index:auto` 的伪元素，
  而 `.big-cover` 是 `z-index:1`、`.cover-bleed` 是 `z-index:0` —— 伪元素与之同层，
  会被封面**整个盖住**，标记一个都看不见。已显式给 `z-index:2`。
  （这类"代码没报错、只是看不见"的问题，光看代码很容易漏，注释里记了原因。）

### 变更

- `app\package.json`：版本号 `1.1.4` → **`1.1.5`**
- `docs\功能说明.md` §8.1：补上第四套主题与「仪器面板变体」的完整说明
- `README.md`：「主题与外观」一节补上莱茵生命，并链到功能说明

### 致谢

配色与设计原则来自 [LBEILC/RhineLabUI](https://github.com/LBEILC/RhineLabUI)（MIT）。
本项目只借用**配色值与设计原则**（细线、直角、竖线刻度语义），
未复制其代码、素材与品牌标识（不使用其 RHINE·LAB logo 字样）。

---

## [1.1.4] — 2025

**主题：** 修复一个会**毁坏用户原始缓存文件**的严重缺陷（硬链接 + 覆盖擦除）。
这是本项目最严重的一次事故，原因、机制与修复完整记录在下面。

### 修复

**★ 严重：擦除派生副本时会就地毁掉用户的原始缓存文件**

事故现象：B 站缓存音频突然无法播放，报 `MediaError 4`（"格式不支持或文件头异常"）。

根因链条：

1. `bili.js` 的 `ensureM4a` 对**嗅探不到 MP4 头**的源文件（`offset === 0`）走的是
   `fs.linkSync(audioPath, dest)` —— **硬链接**，副本与源文件**共享同一个 inode**。
   （当初选硬链接是为了"同盘瞬时、不占额外空间"。）
2. v1.1.0 起副本目录改为**会话临时目录**，退出时由 `wipeDir` 擦除。
3. `wipeFile` 的擦除方式是"以 `r+` 打开、整天覆写 0、`fsync`、再 `unlink`"。
   覆写是**写 inode**，于是透过硬链接直接落到用户的原始缓存文件上；
   随后的 `unlink` 只摘掉副本那个目录项 —— **源文件还在，内容已经全是 0**。
4. 下次启动时 `ensureM4a` 从"已经是全 0"的源文件生成副本，
   Chromium 自然解不出来 → `MediaError 4`。

已实测确认：用户 `Downloads\Mobile Devices\` 下**两个音频缓存文件被原地写成全 0**
（文件大小不变、非零字节数为 0）。带 9 字节自定义头的电脑端缓存不受影响，
因为它们走的是 `copyWithoutPrefix`（真正的独立副本）。

修复（两层，缺一不可）：

- **`wipeFile` 增加硬链接保护**：`nlink > 1` 时**只摘目录项，绝不覆写**。
  这是中央防线 —— 同时保护了会话临时目录与 `purgeLegacyAudioCache()`
  要清理的 `userData\bili_audio`（v1.0.x 在里面留的也正是硬链接）。
- **`ensureM4a` 彻底不再用 `linkSync`**：一律 `copyFileSync` 出**独立副本**
  （`nlink === 1`、与源 inode 不同）。多占一份磁盘，换绝对安全。

### 新增

- `scripts\数据管控验证.js` 增加 **第 3b 组「硬链接保护」回归测试**（断言数 16 → 27）：
  - 构造硬链接副本 → 用**从 `main.js` 源码摘出的真实 `wipeFile`** 擦除 →
    断言源文件 MD5 **一字不变**、链接数回到 1
  - 反向断言 `ensureM4a` 产出的副本不再与源共享 inode、且 `nlink === 1`
  - 静态断言 `wipeFile` 含 `nlink > 1` 保护、`bili.js` 不再出现 `linkSync(` 调用
    （断言前先剥掉注释，避免"注释里提到 `linkSync`"造成误报；
    正则用 `(?<!un)linkSync\(` 以免被 `unlinkSync` 误命中）

### 变更

- `app\package.json`：版本号 `1.1.3` → **`1.1.4`**
- `docs\数据管控与合规.md`：擦除一节补上硬链接保护；「已知边界」补上本次事故的说明
- `docs\架构说明.md`：数据管控层的擦除小节同步

### 说明

- 这两个被毁掉的文件位于 `Downloads\Mobile Devices\`（手机导出的缓存），
  **不在 B 站客户端缓存目录里**，无法通过重新缓存取回。可尝试
  「属性 → 以前的版本」或文件历史找回；否则需从手机重新导出。
- 电脑端 B 站缓存（`Videos\bilibili\`）**全部完好**，因为它们的副本一直是独立文件。

---

## [1.1.3] — 2025

**主题：** 把「文档/更新日志忘了跟着改」变成会失败的检查，并交付一条发布命令。
应用代码（`app/` 下的 js / html）未改动。

### 新增

- `scripts\文档一致性验证.js` —— **16 项文档一致性检查**，几毫秒出结果：
  - `app/package.json` 的版本号在更新日志里有 `## [x.y.z]` 段落，且段落有实质内容（不是占位）
  - README 顶部「当前版本 vX.Y.Z」与 `package.json` 一致
  - 更新日志版本号严格从新到旧、无重复
  - README「项目结构」提到了每一个受版本管理的**顶层条目**，以及 `scripts\`、`docs\` 下的**每一个文件**
  - 反向也查：结构图里不许出现仓库里不存在的"幽灵条目"
  - README「文档」小节与所有 `docs\*.md` 里的相对链接都能解析到真实文件
  - 当前版本已有 git 标签、每个标签都有对应的更新日志段落
- `scripts\release.ps1` —— 一条命令发布：
  `升版本号 → 查更新日志 → 写 package.json/README → 跑三套验证 → 构建 → 提交 → 打标签 → 推送`
  - **第 3 步是强制点**：更新日志里没有新版本段落时，生成骨架并**中止**，等你写完再跑
  - **第 5 步是闸门**：三套验证任一失败就中止，不允许带着文档不一致发布
  - 提交信息不指定时，自动从更新日志该版本的「主题」那行生成
  - `-Bump patch|minor|major` / `-Version x.y.z` / `-NoPush` / `-NoBuild` / `-Yes`
- `docs\维护指南.md` —— 「改什么 → 要同步改哪些文档」对照表、更新日志写作约定、
  提交信息前缀约定、日常节奏

### 变更

- `app\package.json`：版本号 `1.1.2` → **`1.1.3`**
- `README.md`：顶部版本号与结构图补齐新增的脚本与文档，并链接「维护指南」

### 修复

- **`release.ps1` 自身的四个坑**（都是在真跑发布流程时暴露的，记下来避免重犯）：
  - `Get-Content -Raw` 在 PowerShell 5.1 下把**无 BOM 的 UTF-8** 按 GBK 解码，
    `package.json` 的中文描述被破坏后 `ConvertFrom-Json` 直接失败 → 改用显式 `Read-Utf8()`
  - 版本比较函数里 `$A` 与参数 `$a` 是**同一个变量**（PS 变量名不分大小写），
    而 `$a` 声明为 `[string]`，数组赋回去被静默转成 `"1 1 3"`，导致 `$A[2]` 取到字符 `'1'`
    而不是数字 `3` —— 版本比较恒返回 0。参数名与局部名已改成完全不同的名字
  - git / node 把警告与诊断写到 stderr，`EAP=Stop` 下被当成终止性错误而中断脚本
    （表现为"`git add` 把发布流程搞崩了"）→ 新增 `Invoke-Git` 统一封装，临时放宽 EAP
    并自行判退出码
  - 失败后重跑会撞上「版本号必须更大」：因为版本号在验证之前就写进去了。
    已改为**验证失败时回滚版本号**，并允许用同一个版本号**幂等重跑**
- `scripts\数据管控验证.js` 默认应用目录写成了 `__dirname`（即 `scripts\`），
  不带参数运行会找不到 `bili.js`。已改为默认 `..\app`，参数变为可选
- **文档漂移**：检查器一跑就抓出三处真实不一致 —— README 版本号停在 v1.1.1（package.json 已 1.1.2）、
  新脚本没写进结构图。这正是它要防的问题，首次运行就见效
- 「结构图幽灵条目误报」：检测文件名用的正则把长扩展名排在短扩展名之后，
  `package.json` 被 `js` 分支截成 `package.js` → 误报"仓库里不存在的文件"。已把
  `json|html` 排到 `js` 前面
- 「检查器依赖 git 子进程」：原实现用 `execFileSync('git', ...)` 取受管文件与标签，
  而某些受限环境禁止 Node 以管道方式 spawn 子进程（`spawnSync git EPERM`），
  于是检查会**静默退回**文件系统遍历、标签检查则永远读不到。已彻底去掉子进程：
  直接遍历文件系统，直接读 `.git\refs\tags` 与 `.git\packed-refs`

### 文档

- `docs\维护指南.md` 汇总了 PowerShell 5.1 的**五个坑**：BOM、`Get-Content` 编码、
  变量名大小写冲突、原生命令 stderr、`.bat` 里的多字节字符

---

## [1.1.2] — 2025

**主题：推送流程工程化。** 应用代码（`app/` 下的 js / html）未改动，
`app/package.json` 只跟着升了版本号。

### 变更

- `app\package.json`：版本号 `1.1.1` → **`1.1.2`**（应用代码本身无变化）
- `scripts\push-to-github.ps1` 重写：
  - **`-RepoUrl` 变为可选** —— 省略时沿用已有 `origin`，日常更新只需
    `.\scripts\push-to-github.ps1` 一条命令
  - 新增**通道探测 + 自动适配**：先跑 `git ls-remote`，失败时按错误类型分类并兜底
    | 判定 | 兜底动作 |
    | --- | --- |
    | `tls` | 改用 openssl 后端 + 合并本机自签 CA 的 bundle |
    | `credential` | 直接调用 `git-credential-manager.exe` 取缓存凭证，拼成 `Authorization` 头 |
    | `network` / `nonfastforward` | 不做兜底，给出对应提示 |
  - 新增 `-Check`（只探测通道）与 `-NoAutoFix`（关闭兜底）
  - 统一用 `Invoke-GitCapture` 调用 git：临时放宽 `$ErrorActionPreference`，
    避免 git 写在 stderr 的进度信息在 `EAP=Stop` 下被当成终止性错误而中断
- `scripts\setup-push-tls.ps1` 新增 `-SetEnv` / `-EmitPath` / `-Quiet`：
  可把配置直接注入当前进程环境、可只输出 bundle 路径供其它脚本调用
- 新增 `推送.bat`（纯 ASCII 转发），双击即可推送

### 修复

- **「凭证兜底不触发」** —— 通道探测用的是 `git ls-remote`，而**公开仓库的 ls-remote
  根本不需要凭证**，所以 TLS 修好后探测就通过了，脚本误判"通道没问题"，
  一直到真正的 `git push` 才因为认证失败而报错，兜底却已经错过。
  现已把凭证兜底挂到**真正需要认证的那一步**：`push` 失败且判定为
  `credential` / `tls` 类问题时，取缓存凭证重试一次
- 「CA 收集输出绕过 `-Quiet`」：`setup-push-tls.ps1` 里两处裸 `Write-Host`
  不受 `-Quiet` 约束，被 `push-to-github.ps1` 调用时会刷出 50 多行证书清单

### 文档

- `docs\构建与安装.md` 第 6 节重写：单一命令用法、参数表、自动适配的错误分类表、
  标签列表补上 `v1.1.1`；原「推送不通时的排查」降级为原理与手动排查参考
- `README.md` 项目结构补上 `推送.bat` 与新脚本说明

---

## [1.1.1] — 2025

**主题：可视化调音等偏好设置的持久化修复。**

### 修复 —— 设置"重启后不保持"

三个滑块/选项的调音数据本该**全局通用、重启后保持**，实际却会静默丢失。
两类成因都属于"不报错、不崩溃，只是设置悄悄没了"：

- **鼓点强度（写 A 读 B）** — 滑块 `setItem('mp_kick')`，恢复代码却 `getItem('mp_kick2')`，
  而 `mp_kick2` 从来没有被写入过。于是每次重启鼓点强度都退回默认 `0.40`，
  滑块位置也停在中点。已改为读写同一个键 `mp_kick`
- **排序方式（只读不写）** — 恢复代码读 `mp_sort`，但 `#sortSel` 的 `change` 处理器
  只改了变量没落盘。已补上 `setItem('mp_sort', ...)`
- **排序方式（不回填控件）** — 即使读到了值，也没有写回 `<select>`，
  界面仍显示"添加顺序"，看起来就像没保存。已补 `$('#sortSel').value = sm`

顺带加固：排序方式恢复时增加 `SORT_MODES` 白名单校验
（直接信任 localStorage 里的字符串会让下拉框落到空白项）。

### 确认 —— 调音数据本来就是全局的

排查中确认了「所有歌曲通用」这一条**本来就没有问题**，并用自动化断言固定下来：

- 5 个调音参数（`tiltStrength` / `jitterK` / `kickPump` / `bassGainTarget` / `bassSigma`）
  都是模块级全局变量，没有任何一首歌单独持有一套
- 切歌/播放流程（`selectSong` / `playAt` / `playRandom` / `nextSong` / `prevSong`）
  不触碰这些变量
- `idbPut` 写单曲记录时不包含任何调音字段
- 频谱 Worker 只算**原始频段幅度 + 鼓点触发**，不含任何调音参数，
  因此 Worker 重建也不会让调音回退

### 新增 —— 回归验证

- 新增 `scripts\设置持久化验证.js`，**31 项断言**，`node` 直接跑，不需要 Electron：
  | 组 | 验证内容 |
  | --- | --- |
  | 1 | localStorage 键名交叉校验：没有"只读不写"（永远读不到）也没有"只写不读"（写了没人用） |
  | 2 | 每个 `type="range"` 滑块都要「保存 / 恢复同一个键 / 回填控件」三件事齐全 |
  | 3 | 下拉框恢复时回填控件；枚举型设置有白名单校验 |
  | 4 | 调音参数是全局的，且不被切歌流程改动；恢复走 `apply*` 而不是只改变量 |
  | 5 | 调音数据不写进单曲记录（否则就成了"每首歌一套"） |
  | 6 | 恢复时的范围闸门要与滑块 `min`/`max` 一致（改范围忘了改闸门会静默丢弃边界值） |
- 该脚本**验证过有牙齿**：把两个 bug 还原到副本上跑，能精确报出
  「只读未写: mp_kick2 读于第 2299 行 / 只写未读: mp_kick 写于第 1171 行」并退出 1

### 修复 —— 验证脚本自身的缺陷

第 6 组最初用单行正则匹配恢复语句，而 4/5 个滑块的恢复是跨两行的
（`const` 一行、`if` 一行），导致误报"没有范围闸门"。已改为向后看 4 行。

---

## [1.1.0] — 2025

**主题：派生数据管控 + 历史版本回滚。**

### 新增 —— 数据管控

针对程序能解出的两类派生数据（`.ncm` 解出的音频、B 站缓存剥离头后的副本），
加入「不落盘 / 不可导出 / 可追溯」三层管控。

**最小落盘**
- 解密结果只在内存中经 IPC 传递给渲染进程，**从不写入用户音乐目录**
- 新增**会话专属临时目录** `%TEMP%\mp-session-<12位随机ID>\`，每次启动重新生成
- B 站可播放副本改写到会话目录（原来长期驻留在 `userData\bili_audio`）
- 解密子目录在字节读回内存后**立即覆盖擦写并删除**，不等 GC
- `will-quit` 拦截退出 → 递归擦除会话目录 → 真正退出（4 秒兜底，不会关不掉）
- 启动时 `sweepStaleSessions()` 清掉崩溃/强杀残留的会话目录
- 启动时 `purgeLegacyAudioCache()` 擦除 1.0.x 遗留在 `userData\bili_audio` 的长期副本（升级清理）
- 临时目录尽力打上隐藏属性（`attrib +h`）
- `wipeFile()` 覆盖写零 + `fsync` 后再 `unlink`，单会话上限 256 MB 避免退出卡顿

**不可导出**
- 确认全部 27 个 IPC 通道**没有任何一个接受"输出路径"参数**
- `preload.js` 暴露的 16 个方法中**没有任何写文件方法**
- 这条保证是结构性的：不是"没做导出按钮"，而是"没有可用路径把字节写到磁盘"

**可追溯**
- 新增本地审计日志 `userData\audit.log`：时间 / 版本 / 会话 ID / 事件 / 来源文件 / 产物体积 / 结果
- 记录 `ncm-decrypt`、`bili-extract`、`bili-scan` 三类事件，上限 500 条自动裁剪
- 只在本机，程序从不外发

**输入侧限制**
- `convert-ncm` 先校验 `CTENFDAM` 容器魔数，非 NCM 直接拒绝（不再是一个通用解密器）
- B 站提取只处理本机已有缓存，不发起任何音频下载请求
- 原始 `.ncm` 与缓存文件从不被修改或删除

**程序内可见性**
- ⚙ 面板新增「数据管控」行：`内存解密 · 会话级临时目录 · 无导出 · 审计 N 条`（悬停看完整说明与审计路径）
- 同排「清日志」按钮一键清空审计
- ⚙ 面板新增「版本」行，显示当前版本号
- 首次启动弹出一次**使用边界与数据管控告知**弹层（记录在 `localStorage.mp_notice_v110`）

**新增 IPC / API**
- `get-data-policy` → `{ version, sessionId, auditCount, policy }`
- `clear-audit-log`
- `preload` 对应暴露 `getDataPolicy()` / `clearAuditLog()`

**回归验证**
- 新增 `scripts\数据管控验证.js`，16 项断言：
  副本落盘位置（3 项）、擦除逻辑（3 项）、陈旧清扫（1 项）、静态断言（8 项）、
  preload 暴露面（1 项）。`node scripts\数据管控验证.js app` 即可跑
- 擦除逻辑的测试**从 `main.js` 源码里摘出实现**再执行，测的是真实代码文本而非复制品

**文档**
- 新增 `docs\数据管控与合规.md`：技术实现、验证明细、使用边界、已知边界（含"覆盖擦除不保证
  取证级不可恢复"的诚实说明）
- README 新增「数据管控」一节；`docs\功能说明.md` 的 NCM / B 站章节同步更新

### 新增 —— 历史版本回滚

- `versions\` 归档 12 个历史关键节点的逐字节快照（自包含单文件 HTML，可直接当 `app/index.html` 运行）
- `scripts\use-version.ps1` 一键回滚：自动备份当前版本 → 替换 → 重新构建
- `versions\README.md` 含版本对照表、兼容性说明，以及一段已被后续需求取代的历史约束记录

### 变更

- `app\package.json`：版本号 `1.0.0` → **`1.1.0`**，补充 `author` / `repository`，标记 `private`
- 新增 git 附注标签 **`v1.0.0`**（首个公开发布版本）与 **`v1.1.0`**（本版本），
  推送时由 `push-to-github.ps1` 一并推送
- B 站可播放副本位置：`userData\bili_audio\` → **会话临时目录**（下次启动由 `ensureAllPlayable()` 重新生成）
- `bili.js` 的 `ensureM4a(audioPath, cacheDir, force)`：第二个参数语义由 `userDataDir` 改为
  **直接的缓存目录**；`scan` 新增 `opts.audioDir`（未提供时回退旧行为）
- `main.js` 的 `cleanupDir()`（`fs.rmSync`）→ `wipeDir()`（覆盖擦写 + 删除）

### 修复

- 「推送脚本把失败当成功」：`scripts\push-to-github.ps1` 未检查 `git push` 退出码，
  而 PowerShell 5.1 的 `$ErrorActionPreference='Stop'` 不覆盖原生命令退出码 ——
  推送失败也会打印"推送成功"。现已显式检查并给出针对性排查建议
- 「bat 注释被当成命令执行」：`构建.bat` / `安装.bat` 在 `chcp` 之前含中文注释，
  cmd.exe 切换代码页后按旧字节偏移重读文件，把 `rem` 行解析成命令并报错。
  已改为纯 ASCII；生成的卸载脚本改为 `.bat`（纯 ASCII 转发）+ `.ps1`（真正逻辑）
- 「失效的代码注释」：`app\index.html` 中引用了已删除的调试脚本路径

---

## [1.0.0] — 2025

首个公开发布版本。

### 新增

**曲库**
- 支持文件导入、文件夹递归导入、拖拽导入
- 支持 `mp3` / `flac` / `m4a` / `aac` / `wav` / `ogg` / `wma` / `ape` / `ncm`
- `.ncm` 网易云加密文件导入时自动解密（内置 `ncmdump.exe` + AES-ECB 回退实现）
- B 站客户端缓存扫描与音频提取
- 播放列表拖动排序、「下一首播放」队列插入

**元数据**
- 标题 / 歌手 / 专辑 / 封面自动识别
- 内嵌标签优先，回退同目录图片与文件名解析
- 多歌手识别：`/`、`、`、`&`、`;`、`feat.`、`ft.`、`with`、`×` 等分隔符全部拆分，并保护 `AC/DC`、`R&B` 等已知组合
- 已导入歌曲一键「重新识别」，且不丢失收藏、播放次数与进度

**播放**
- 四种播放模式：列表循环 / 顺序播放 / 单曲循环 / 随机播放
- 0.5× ~ 2.0× 倍速播放
- 播放进度、音量、播放模式、当前曲目自动记忆并恢复
- 关闭时正在播放的话，下次启动恢复到暂停状态（不突然出声）

**歌词**
- 同名 `.lrc`、内嵌歌词、相似文件名匹配三种来源
- UTF-8 / UTF-8 BOM / GBK 编码自动识别
- 逐行滚动高亮，支持翻译行

**频谱可视化**
- 自研频谱分析器：Hann 窗 + Goertzel 精确取点，1024 点时域样本
- 对数频率分带，120 条柱体
- 峰值归一化 + 对数压缩，安静段与爆音段都有动态
- 鼓点泵浦：低频能量动态参考值 + 上升沿检测 + 乘性调制
- 高耸条自动定位到柱体序列左边缘，配合倾斜因子形成窄而尖的形态
- 相干行波叠加（约 12 条/秒，方向右 → 左）营造滑动感
- 分析运行在 Web Worker 中，Worker 不可用时自动降级为同步计算
- 定时器驱动渲染，窗口被遮挡 / 最小化时不被 Chromium 节流
- 可视化参数（灵敏度、倾斜、鼓点强度、峰值保持、低频宽度、滑动强度、柱体粗细）可在设置面板实时调整

**迷你悬浮窗**
- 复用主窗已算好的柱体数据，不重复计算
- 使用最高置顶层 `screen-saver`，可显示在无边框全屏游戏之上
- 点击不抢焦点（`setFocusable(false)`），游戏中操作不切出
- 默认点击穿透，光标悬停到控件上时才可点
- 支持拖动（主进程光标轮询，尺寸锁定）、缩放、透明度、锁定、边缘吸附

**系统集成**
- 媒体键 / 耳机线控：`MediaPlayPause` / `MediaNextTrack` / `MediaPreviousTrack` / `MediaStop` / `MediaPlay` / `MediaPause`
- 双通道接管：Chromium Media Session（SMTC）+ Electron `globalShortcut`
- 幂等注册 + 6 s 自动重试，避免把已注册的键误判为「被占用」
- 300 ms 防抖，避免耳机连发事件导致播放/暂停互相抵消
- 设置面板内置媒体键自检（✓ 已注册 / ⚠ 被占用 / ✕ 全部被占用）
- 全局备用组合键 `Ctrl+Alt+P/N/B/Space`
- 迷你窗全局控制键 `Ctrl+Alt+方向键/0/9/L`

**界面**
- 左右分栏布局：左侧大封面，右侧歌曲信息 + 歌词 + 频谱可视化
- 封面区域向整屏渗出模糊背景，形成类网易云全屏封面的渐变过渡
- 三套主题：深色（蓝紫）、浅色、网易云红（`#EC4141`）
- 所有弹层、滑槽、滑块、进度条把手跟随主题
- 统一极简线性图标（`stroke-width: 1.7`，圆角端点），圆角播放按钮与胶囊形暂停图标
- 收藏、播放次数统计、多字段排序

**工程**
- `scripts/build-portable.ps1` — 一键构建便携版，运行时本地复用或自动下载
- `scripts/install.ps1` — 按用户安装 + 桌面/开始菜单快捷方式 + 卸载脚本
- `scripts/use-version.ps1` — 回滚 `app/index.html` 到 `versions/` 里的历史快照
- `scripts/push-to-github.ps1` — 设置 origin 并推送，含退出码检查与失败归因
- `versions/` — 12 个历史版本快照（自包含单文件 HTML，可直接当 `app/index.html` 运行）
- `构建.bat` / `安装.bat` 双击即用
- 完整中文文档：功能介绍、快捷键、构建与安装、架构说明、版本回滚

### 修复

开发过程中定位并修复的问题，一并记录以便追溯：

- **鼓点响应失效** — 低频参考值 `kickRef` 只在首次赋值（`if (kickRef <= 0)`）之后不再更新，
  导致 `kick` 迅速饱和到 1.0，乘性泵浦完全失效。改为持续跟踪 `kickRef += (lowMag - kickRef) * 0.05`，
  Worker 与同步回退两条路径都修复
- **滑动感做不出来** — 值位移混合在平滑频谱上不可见；随机抖动波方向随机、平均位移为零。
  改为每 6 条柱体一个周期的相干行波，相位每帧推进 0.26
- **打开小窗后主窗帧率减半** — `resources\app\main.js` 曾设置
  `webContents.setFrameRate(60)` 且小窗持续渲染；移除 `setFrameRate`，
  小窗改为按需渲染，并关闭 `disable-direct-composition`（保留 flip-model）
- **媒体键状态误报「被占用」** — 6 s 重试逻辑把已被本程序注册的键重新注册，
  Electron 返回 `false`（`IsRegistered` 检查），把健康状态改写成失败。改为幂等注册
- **进度条圆形滑块不跟随** — 代码只设置了 `#barFill` 的宽度，`#barThumb` 从未被定位。
  新增 `setBarUI(pct)` 同时设置填充宽度与滑块位置，接入 `timeupdate`、重置与两条拖动路径
- **暂停按钮看起来是方的** — 两个独立原因：图标原来是两个尖角矩形
  （`M6 5h4v14H6zM14 5h4v14h-4z`），改为圆角 `<rect rx="1.8">`；
  且 `#playBtn` 缺少 `icon-btn` class，因而没有 `border-radius: 50%`
- **迷你窗图标未同步改版** — 主界面换新图标集后小窗仍用旧图标，新增独立 `MIC` 图标库
- **迷你窗缩放异常** — 渲染层 `e.screenX` 已是 DIP，又除了一次 `scaleFactor`，
  125% 缩放下窗口增长慢 20%。改用主进程 `screen.getCursorScreenPoint()`
- **迷你窗拖不动** — 点击穿透靠页面 mousemove 事件驱动，快速移动后立刻按下时状态未切换。
  改为主进程 50 ms 光标轮询，即使丢事件也会自愈
- **迷你窗被游戏遮挡** — 无边框全屏游戏会重新抢 Z 序，增加 800 ms 周期重申置顶

### 已知限制

- Windows 独占全屏（Exclusive Fullscreen）下，任何普通窗口都无法覆盖，迷你窗不会显示；
  请将游戏改为「无边框窗口」或「窗口化全屏」
- Windows 媒体键是独占资源，被其它程序注册时本程序无法接管，设置面板会显示占用状态
- 60 Hz 显示器下可见帧率上限为 60，渲染路径的目标是「稳定无卡顿」而非追求更高数字

---

## 版本规划

### 计划中

- 播放列表导入 / 导出（M3U）
- 自定义封面替换
- 均衡器
- 多语言界面

### 暂不考虑

- 在线音乐源（本项目定位为本地播放器）
- 云同步
